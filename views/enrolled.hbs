{{> viewCourseHeader }}
<body class="profile-page">
    {{#if session.user }}
      {{> navRegistered }}
    {{/if}}
    <div class="wrapper">
        <div class="section">
            {{> pageEnrolledCourse }}
        </div>
        {{> genericFooter }}
    </div>
    {{> scripts }}
</body>
</html>
